result

# devenv's per-project state: the shell profile, its gc roots and process logs
.devenv/
.devenv.flake.nix

# Uncommitted per-machine overrides layered on top of devenv.nix
devenv.local.nix

build/
build-*/
install/

# Per-worktree state written by the tasks (dependency pins, build locks)
.hlsl-dev/

# Worktrees created with `wt` sit next to the submodule they belong to
/llvm-project.*
/DirectXShaderCompiler.*
/offload-test-suite.*
/offload-golden-images.*
.cache/
*.o
*.a
*.so
*.dylib
*.dll
*.exe
a.out
.sccache/
vkd3d-proton.cache
compile_commands.json
.clangd/
.vs/
.vscode/
.idea/
*.local.properties
.env

# offloader-scripts tooling artifacts
reports/
_site/
__pycache__/
