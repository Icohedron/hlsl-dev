# dotenv_if_exists loads local overrides (e.g. HLSL_VK_DRIVER, written by
# `mask vk-use`) before the flake, so the shellHook can read them. It also
# registers .env with direnv's watcher, so edits trigger an automatic reload.
dotenv_if_exists .env
use flake
